{
  "name": "ChatWave",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:public && npm run build:server",
    "build:public": "cd public && npm install && npm run build",
    "build:server": "cd server && npm install && tsc -b",
    "start": "node server/index.js"
  },
  "author": "",
  "license": "ISC"
}
